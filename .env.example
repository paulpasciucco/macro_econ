# ── API Keys for Economic Statistics Viewer ──
#
# Copy this file to .env and fill in your keys:
#   cp .env.example .env
#
# The .env file is git-ignored and will not be committed.
#
# All three APIs are free. Registration takes ~1 minute each.

# FRED (Federal Reserve Economic Data) — required for most notebooks
# Register: https://fred.stlouisfed.org/docs/api/api_key.html
FRED_API_KEY=

# BEA (Bureau of Economic Analysis) — required for NIPA table detail
# Register: https://apps.bea.gov/API/signup/
BEA_API_KEY=

# BLS (Bureau of Labor Statistics) — optional but recommended
# Without a key, falls back to v1 (25 series/request, 10 years max).
# With a key, uses v2 (50 series/request, 20 years max).
# Register: https://data.bls.gov/registrationEngine/
BLS_API_KEY=
